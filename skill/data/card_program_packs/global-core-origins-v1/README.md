# global-core-origins-v1

The first global card-program pack under ADR-0004. Status: **selected, pending
physical verification; inventory drafted; no executable programs.**

| File | What it is | Written by |
| --- | --- | --- |
| `selection.json` | Which decks and why, with the capability batches R3-A0..A3 | Codex, `r3-pack-selection.v1` |
| `inventory.draft.json` | R3-A0 output: every selected card split into stable clauses, labelled, as a `card-behavior-manifest.v1` with `status: draft` | generated by `build_r3_inventory.py` |
| `inventory_ledger.json` | Per card: which printing was read, whether errata applies, whether the snapshot still carries pre-errata text, the mechanics each clause needs, and which batch unblocks it | generated alongside |
| `INVENTORY.md` | The same ledger as prose, per deck and per mechanic — the view R3-A1 rulings read | generated alongside |
| `r3a1_clause_ledger.json` / `R3A1_LEDGER.md` | C-13: every R3-A1 clause with its Core sub-clause locators (quoted), whether it targets under 355.10, and where its decision is made | generated by `build_r3a1_ledger.py` |
| `r3a1_fixture_drafts.json` | C-13: four drafts per clause — positive, negative, missing information, target invalidated — as state sketches over `base_state()`. The shape has no expected-outcome field | generated alongside |
| `decision_packets.json` / `DECISION_PACKETS.md` | C-13: the questions X-09 must answer once for all eleven cards, each with rule text on both sides and a proposed failure classification | generated alongside |
| `r3a1_programs.json` | C-18: one executable program per clause the engine can now run, its cost declaration, and fixtures whose expected outcomes cite the Core clause they come from. The only hand-written executable artifact in the pack | Claude, `r3a1-card-programs.v1` |
| `r3a1_behavior_manifest.json` / `R3A1_PROGRAMS.md` | C-18: a `card-behavior-manifest.v1` (still `draft`) whose clause statuses are *derived* by `r3a1_programs.py` from those fixtures passing — a claim never becomes a status on its own; stale inventory clauses stay stale with no program | generated by `r3a1_programs.py` |

## What the inventory can and cannot say

The behavior-manifest validator requires a tested program for any clause
labelled `full` or `partial`. R3-A0 builds no programs, by rule. So every
clause in `inventory.draft.json` is `unsupported` — with its missing mechanics
named explicitly — or `stale`, where the bundled card text still carries
pre-errata wording. The `full`/`partial` labels the batch asks for are drafted
as **recommendations** in the ledger, never written into the manifest where
they would mean something that has not happened.

`current_text_hash` is computed by the same catalog Deck Coach uses, with the
same errata applied, so a future active manifest and today's draft agree on
what "current text" means.

Nothing here activates. The inventory keeps `status: draft` with no
`program_id`; the C-18 manifest is also `draft` and carries a `program_id`
only where a positive and a negative fixture pass against the engine. Card
status is `full` only when every clause is; a stale card stays stale until a
human reverifies its text. Activation is a separate ADR-0004 gate.
